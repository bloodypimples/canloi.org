<h1>Các bài đã đăng</h1>

<%= link_to 'Đăng bài mới', new_post_path %>

<% @posts.each do |post| %>
  <%= render 'post', post: post %>
<% end %>

<% if @posts.count >= 10 %>
  <div class="pages-wrapper">
    <span>Trang:</span>
    <%= will_paginate @posts %>
  </div>
<% end %>
